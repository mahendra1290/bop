#include<iostream>

using namespace std;

class Array{
    int *arr;
    int len;
    public:
        Array(int len);
        Array(Array &arr1);
        ~Array();
};

Array::Array(int elemCount){
    arr = new int[elemCount];
    len = elemC;
}

Array::Array(Array &arr1){
    int i;
    cout <<arr1.len<<endl;
    arr = new int[10];
    len = arr1.len;
    //for(i=0; i<len; i++){
       // arr[i] = arr1.arr[i];
   // }
}

Array::~Array(){
    cout <<"destructor"<<endl;
}

int main(){
    Array arr_1(10);
    Array arr_2(arr_1);
}
